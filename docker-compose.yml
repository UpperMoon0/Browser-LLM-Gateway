version: '3.8'

services:
  gateway:
    build: .
    ports:
      - "11436:11436"
    volumes:
      - ./.data/chatgpt-profile:/app/.data/chatgpt-profile
    environment:
      - HOST=0.0.0.0
      - PORT=11436
      - HEADLESS=true
    restart: unless-stopped
